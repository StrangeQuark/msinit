# Application name
spring.application.name=${SPRING_APPLICATION_NAME:fileservice}

# Server port
server.port=${SERVER_PORT:6010}

# Logging
logging.level.com.example=${LOGGING_LEVEL:INFO}
service.http.connect.timeout=${SERVICE_HTTP_CONNECT_TIMEOUT:5000}
service.http.read.timeout=${SERVICE_HTTP_READ_TIMEOUT:10000}

# DB config
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}
spring.jpa.database=${SPRING_JPA_DATABASE:postgresql}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}

# Multipart file
spring.servlet.multipart.enabled=${SPRING_SERVLET_MULTIPART_ENABLED:true}
spring.servlet.multipart.max-file-size=${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:-1}
spring.servlet.multipart.max-request-size=${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:-1}

# File storage
file.storage.path=${FILE_STORAGE_PATH:uploads}
file.deletion.retry.delay=${FILE_DELETION_RETRY_DELAY:60000}
file.reconciliation.min.age=${FILE_RECONCILIATION_MIN_AGE:3600000}

# CORS origins
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:6080}

# SECRET KEYS
ENCRYPTION_KEY=${ENCRYPTION_KEY}
JWT_PUBLIC_KEY=${JWT_PUBLIC_KEY}
JWT_ISSUER=${JWT_ISSUER}
SERVICE_SECRET_FILE=${SERVICE_SECRET_FILE}
authservice.integration=${AUTHSERVICE_INTEGRATION}
telemetryservice.integration=${TELEMETRYSERVICE_INTEGRATION}
