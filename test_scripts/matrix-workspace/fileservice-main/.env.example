# App and server config
SPRING_APPLICATION_NAME=fileservice
SERVER_PORT=6010
CORS_ALLOWED_ORIGINS=http://localhost:6080,http://localhost:8080,http://react-service
LOGGING_LEVEL=INFO

# DB Config
SPRING_DATASOURCE_URL=jdbc:postgresql://file-db:5432/fileservice
SPRING_DATASOURCE_DRIVER=org.postgresql.Driver
POSTGRES_USER=change-me
POSTGRES_PASSWORD=change-me
POSTGRES_DB=fileservice

# JPA / Hibernate
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=true
SPRING_JPA_DATABASE=postgresql
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.PostgreSQLDialect

# Multipart file configuration
SPRING_SERVLET_MULTIPART_ENABLED=true
SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=-1
SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=-1
FILE_STORAGE_PATH=/fileservice/uploads
FILE_DELETION_RETRY_DELAY=60000
FILE_RECONCILIATION_MIN_AGE=3600000

# JWT keys
JWT_PUBLIC_KEY=change-me
JWT_ISSUER=msinit-authservice

# Secret keys
# Generate these secret keys with the following linux shell command: head /dev/urandom | tr -dc A-F0-9 | head -c32
ENCRYPTION_KEY=change-me
SERVICE_SECRET_FILE=change-me
SERVICE_HTTP_CONNECT_TIMEOUT=5000
SERVICE_HTTP_READ_TIMEOUT=10000
AUTHSERVICE_INTEGRATION=true
TELEMETRYSERVICE_INTEGRATION=true
